# ════════════════════════════════════════════════════════════════════
#  PUBLIC — full indexing enabled at launch.
#
#  History: pre-launch this file gated the major search crawlers to two
#  gift-guide URLs while the site was in early release. That block has been
#  removed. The whole public surface is now crawlable; admin tooling stays out.
# ════════════════════════════════════════════════════════════════════

# Cloudflare Managed robots.txt prepends a global `User-agent: * / Allow: /`
# group in production, plus its AI-crawler Content-Signal directives. The group
# below is the origin's own public rule and keeps admin pages out of the index.
User-agent: *
Allow: /
Disallow: /admin.html
Disallow: /catalog-admin.html

# Canonical XML sitemap Google + Bing want. The gift-guide URLs that used to
# live in a separate sitemap-gift-guides.xml are now folded into sitemap.xml.
Sitemap: https://smuthub.ca/sitemap.xml
