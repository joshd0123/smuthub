# ════════════════════════════════════════════════════════════════════
#  ⚠️  EARLY LIVE RELEASE — CRAWLING IS DISABLED ON PURPOSE
#
#  smutHub is live for invited/early users, but search discovery is not being
#  opened yet. Keep the global block below until the public indexing launch is
#  explicitly approved.
#
#  Left crawlable, search engines could index an early release before launch
#  QA, canonical-host checks, and the public sitemap rollout are complete.
#
#  ►►► ON INDEXING LAUNCH DAY: delete the Disallow line below, then uncomment the
#      block underneath. See LAUNCH.md step 4.
#      Skipping this leaves the live site invisible to search engines,
#      and nothing on the site will look broken — so it is easy to miss.
# ════════════════════════════════════════════════════════════════════

User-agent: *
Disallow: /

# ── Restore at launch (and delete the two lines above) ───────────────
# smutHub — public crawler rules
# Allow all bots into the public surface; keep admin tooling out of the index.
# User-agent: *
# Allow: /
# Disallow: /admin.html
# Disallow: /catalog-admin.html
#
# Sitemap (canonical XML index Google + Bing want)
# Sitemap: https://smuthub.ca/sitemap.xml
